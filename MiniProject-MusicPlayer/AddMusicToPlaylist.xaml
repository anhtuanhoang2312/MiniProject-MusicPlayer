<Window x:Class="MiniProject_MusicPlayer.AddMusicToPlaylist"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MiniProject_MusicPlayer"
        mc:Ignorable="d"
        Title="AddMusicToPlaylist" Height="450" Width="800">
    <Grid>
        <StackPanel>
            <ListView Name="PlayListsListView" Height="383">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Button Name="AddButton" Content="Add" Click="AddButton_Click" Width="50" Height="30"/>
        </StackPanel>
    </Grid>
</Window>
