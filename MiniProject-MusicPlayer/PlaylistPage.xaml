<UserControl x:Class="MiniProject_MusicPlayer.PlaylistPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MiniProject_MusicPlayer"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TextBlock Text="Playlist" FontSize="40" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        <!--<ListView>
            <ListView.View>
                <GridView>
                    <GridView.ColumnHeaderContainerStyle>
                        <Style>
                            <Setter Property="FrameworkElement.Visibility" Value="Collapsed"/>
                        </Style>
                    </GridView.ColumnHeaderContainerStyle>
                    <GridViewColumn Header="Name" Width="120" DisplayMemberBinding="{Binding Title}" />
                    <GridViewColumn Header="Age" Width="50" DisplayMemberBinding="{Binding Artist}" />
                    <GridViewColumn Header="Mail" Width="150" DisplayMemberBinding="{Binding Album}" />
                </GridView>
            </ListView.View>
</ListView>-->

    </Grid>
</UserControl>
